
Static nginx port with zlib, pcre, openssl and perl.

USAGE
    make tarball
	Download and extract sources, apply all the patches
	and produce a tarball of the port to use with buildbots:
	staticnginx-1.8.0.1.tar.gz

    make
	Build binaries. Missing sources will be downloaded,
	extracted and patched.

    make release
	Package binaries into a tarball:
	staticnginx-1.8.0.1-bin.tar.gz

    make checkupdates
	Check for updates.

